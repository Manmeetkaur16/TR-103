# 17 July - Daily Diary

## 📘 Topics Covered Today

* **Updating Data in Database (PUT & PATCH Requests)**

  * Learned how to update existing records in the database using Express.js and MongoDB  
  * Understood the difference between:
    * **PUT** → updates the **entire document**
    * **PATCH** → updates **only specific fields**
  
  * Practiced using routes like:
    ```js
    app.put("/user/:id", async (req, res) => {
      // replace entire user document
    });

    app.patch("/user/:id", async (req, res) => {
      // update specific fields
    });
    ```
  
  * Understood real-world applications:
    * Updating user profile  
    * Editing product details  
    * Changing status fields  

  * Used Mongoose functions such as:
    * `findByIdAndUpdate()`  
    * `{ new: true }` to return updated data  
    * `$set` operator for partial updates  

---

* **Deleting Data Using DELETE Request**

  * Learned how to remove data from MongoDB using the DELETE HTTP method  
  * Practiced building a backend route:
    ```js
    app.delete("/user/:id", async (req, res) => {
      // delete a user from database
    });
    ```

  * Understood use cases:
    * Deleting a user account  
    * Removing products  
    * Clearing data records  

  * Used Mongoose methods like:
    * `findByIdAndDelete()`  
    * `deleteOne()`  
    * `deleteMany()`  

  * Learned how to send a proper response after deletion:
    * Success message  
    * Error handling if record not found  

