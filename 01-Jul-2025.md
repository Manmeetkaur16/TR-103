# 1 July - Daily Diary

## 📘 Topics Covered Today

* **Project: Basic To-Do Page (HTML + CSS)**

  * **Creating the Structure Using HTML**
    * Designed the basic layout of a To-Do application
    * Added essential HTML elements:
      * Main heading (`<h1>`)
      * Input field for entering tasks
      * Add button for future functionality
      * Unordered list (`<ul>`) to display tasks
    * Ensured a clean and semantic structure for better styling and future JavaScript integration

  * **Styling the To-Do App Using CSS**
    * Applied CSS to improve the UI design of the To-Do application
    * Styled components such as:
      * Page container  
      * Input box  
      * Add button  
      * Task list items  
    * Used basic CSS properties:
      * Background colors, padding, margin  
      * Borders, border-radius  
      * Fonts, spacing, layout alignment  
    * Ensured the overall design looks neat, centered, and user-friendly

  * **No JavaScript Yet — Only UI**
    * Focused only on the visual structure and styling
    * Prepared the UI so JavaScript can be added later to make it fully functional


* **Display Grid in Bootstrap**
  
  * Explored how Bootstrap’s **grid system** helps in creating responsive and structured page layouts  
  * Understood how the grid is divided into **12 equal columns**, allowing flexible combinations for layout design  

* **Using the `.row` Class**
  
  * Learned how to group columns using the `.row` class  
  * The `.row` ensures proper alignment and spacing between columns  
  * Practiced organizing elements inside a row for clean horizontal layout structure  

* **Using the `.col` Class**
  
  * Used the `.col` class to specify column widths  
  * Understood how columns automatically adjust width if no size is specified  
  * Practiced layouts like:
    * `.col-6` + `.col-6`
    * `.col-4` + `.col-4` + `.col-4`
    * Simple equal-width columns using `.col`  

* **Responsive Design Properties**
  
  * Discussed Bootstrap’s responsive breakpoint classes:
    * `col-lg-*` for large screens  
    * `col-md-*` for medium screens  
    * `col-sm-*` for small screens  
    * `col-xs-*` / default for extra small screens  
  * Learned how responsive classes help create different layouts on different screen sizes  

