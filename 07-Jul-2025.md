# 7 July - Daily Diary

## 📘 Topics Covered Today

* **DOM Manipulation**

  * Learned how JavaScript interacts with the HTML structure using the **Document Object Model (DOM)**  
  * Understood how DOM manipulation allows dynamic changes to content, styles, and structure of a webpage  

* **Selecting Elements**

  * Practiced different DOM selection methods:
    * `document.getElementById()`  
    * `document.getElementsByClassName()`  
    * `document.getElementsByTagName()`  
    * `document.querySelector()`  
    * `document.querySelectorAll()`  
  * Used these methods to target specific elements for manipulation  

* **Changing Text, Styles, Attributes**

  * Modified text content using:
    * `.innerText`  
    * `.innerHTML`  

  * Changed CSS styles using:
    * `element.style.color`  
    * `element.style.backgroundColor`  
    * `element.style.fontSize`  

  * Updated attributes using:
    * `setAttribute()`  
    * `getAttribute()`  
    * `removeAttribute()`  

  * Practiced real examples like changing button text and updating image source  

* **Adding / Removing Elements**

  * Created new elements using:
    * `document.createElement()`  
    * `appendChild()`  
    * `append()`  
    * `prepend()`  

  * Removed or replaced elements using:
    * `.remove()`  
    * `.removeChild()`  
    * `.replaceChild()`  

  * Built small dynamic lists by adding/removing items  

* **Event Listeners**

  * Attached events using:
    * `.addEventListener("click", ...)`  
    * `.addEventListener("input", ...)`  
    * `.addEventListener("submit", ...)`  

  * Understood how events trigger functions and help create dynamic UI interactions  
  * Practiced click events, key events, and form submission  

* **Added JavaScript Functionality to To-Do App**

  * Integrated DOM manipulation concepts into the To-Do project  
  * Achieved:
    * Adding new tasks dynamically  
    * Showing tasks inside the list  
    * Removing tasks when clicked  
    * Clearing input field after adding a task  
  * Made the To-Do App fully functional using JavaScript  

